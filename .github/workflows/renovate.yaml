name: "Maintenance: Renovate"

on:
  push:
    branches:
      - main
    paths:
      - .renovaterc.json5
  schedule:
    - cron: '0 * * * *' # Every hour
  issues:
    types:
      - edited
  workflow_dispatch:
    inputs:
      dryRun:
        description: Dry Run
        type: boolean
        default: false
      logLevel:
        description: Log Level
        default: debug
      version:
        description: Renovate Version
        default: latest

jobs:
  renovate:
    permissions:
      contents: write
      issues: write
      pull-requests: write
    uses: alexander-zimmermann/lares/.github/workflows/sidecar-renovate.yaml@2f640f1d299402e0ccf38957b93f8ca3681687ae # main
    with:
      dryRun: ${{ inputs.dryRun && 'full' || '' }}
      logLevel: ${{ inputs.logLevel || 'debug' }}
      version: ${{ inputs.version || 'latest' }}
    secrets: inherit
